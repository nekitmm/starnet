# To run: conda env create -f environment-lnx-cuda.yml
name: starnet
channels:
    - conda-forge
dependencies:
    - python=3.9
    - pip
    - Pillow
    - ipython
    - matplotlib
    - numpy
    - tifffile
    - tensorflow